//const largeFolder = { name: "", children: [{ name: "node", children: [] }] };
//
//for (let i = 0; i < 100; i++) {
// largeFolder.children[0].children[i] = { name: `${i + 1}`, children: [] };
// for (let j = 0; j < 100; j++) {
// largeFolder.children[0].children[i].children[j] = {
// name: `${i + 1} - ${j + 1}`
// };
// }
//}
